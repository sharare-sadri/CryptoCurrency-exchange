replicaCount: 2

image:
  repository: sharare-sadri/crypto-exchange
  pullPolicy: IfNotPresent
  tag: "0.0.1" 

service:
  type: ClusterIP
  port: 8000

ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: crypto.example.com
      paths:
        - path: /
          pathType: ImplementationSpecific

# We pass secrets from the CI/CD pipeline, not storing them here.
secrets:
  djangoSecretKey: "Wyy4ZwsMLW-Uoc-fMJLLTFisnJj7gM8ZWp0dkySs1x1HlpJpvIAhCElaxM7pO8HXIlU"
  dbPassword: "P@ssw0rd123%"

# Database connection details
database:
  host: "postgresql-service.default.svc.cluster.local"
  port: "5432"
  user: "crypto_user"
  name: "crypto_db"